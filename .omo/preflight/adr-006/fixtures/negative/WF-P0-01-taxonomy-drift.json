{
  "contract_id": "WF-P0-01",
  "kind": "negative",
  "scenario": "taxonomy-drift",
  "assertion": "A module outside the canonical taxonomy is rejected.",
  "expected_failure_id": "taxonomy_drift",
  "mutation": {
    "type": "violate_constraint",
    "target": "$.modules.0.name",
    "payload": {
      "invalid_value": "custom_module"
    },
    "comment": "Replaces a canonical module name with a non-canonical module — taxonomy drift"
  }
}
