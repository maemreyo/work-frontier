{
  "contract_id": "WF-P0-05",
  "kind": "negative",
  "scenario": "stale-outbox-fingerprint",
  "assertion": "An outbox write with a stale projection fingerprint is rejected.",
  "expected_failure_id": "stale_outbox_fingerprint",
  "mutation": {
    "type": "violate_constraint",
    "target": "$.outbox_fingerprint",
    "payload": {
      "invalid_value": "fp-stale"
    },
    "comment": "Outbox fingerprint diverges from projection fingerprint — stale write rejected"
  }
}
