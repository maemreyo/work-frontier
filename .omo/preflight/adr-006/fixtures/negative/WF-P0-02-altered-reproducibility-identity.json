{
  "contract_id": "WF-P0-02",
  "kind": "negative",
  "scenario": "altered-reproducibility-identity",
  "assertion": "A replay whose identified input differs is rejected.",
  "expected_failure_id": "malformed_source_revision_set",
  "mutation": {
    "type": "violate_constraint",
    "target": "$.source_revision_set",
    "payload": {
      "invalid_value": "altered-sha"
    },
    "comment": "Replaces source_revision_set object with a string — identity structure broken"
  }
}
