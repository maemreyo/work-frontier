{
  "name": "work-frontier",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": "22.23.1",
    "pnpm": "10.20.0"
  },
  "scripts": {
    "check": "pnpm --dir frontend run check",
    "fix": "pnpm --dir frontend run fix",
    "test": "pnpm --dir frontend run test",
    "test:preflight": "node --test .omo/preflight/adr-006/validate.test.mjs",
    "typecheck": "pnpm --dir frontend run typecheck"
  },
  "devDependencies": {
    "ts-morph": "^28.0.0",
    "x-to-zod": "0.10.3"
  }
}
