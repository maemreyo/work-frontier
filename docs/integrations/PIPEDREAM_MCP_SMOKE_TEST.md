# Pipedream MCP smoke test

This temporary documentation-only change verifies that the ChatGPT Pipedream GitHub MCP connector can create a branch, commit a file change, and open a pull request.

It does not change application behavior and can be removed after verification.

Do not commit to main.